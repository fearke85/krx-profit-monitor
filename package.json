{
  "name": "krx-profit-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard local de rentabilidade da mineração KERYX (KRX)",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c green,cyan \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
