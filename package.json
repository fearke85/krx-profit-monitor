{
  "name": "krx-profit-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard de rentabilidade da mineração KERYX (KRX) — front-only + IndexedDB",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "preview": "npm run preview -w web"
  }
}
