server {
    listen 80;
    server_name krx-profit-monitor;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://server:4000;
        proxy_set_header Host $host;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
