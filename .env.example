# Opcional — só a function /api/price na Vercel usa isto.
# Default já aponta para o ticker KRX_USDT da nonkyc (URL pública, sem API key).
# Se sobrescrever, o host DEVE ser nonkyc.io (allowlist anti-SSRF no handler).
# NONKYC_URL=https://nonkyc.io/api/v2/market/getbysymbol/KRX_USDT
